Information Technology

FIT3143 Parallel Computing
Semester 2 2024

Topic 8:
Introduction to GPUs and NPUs

Dr Carlo Kopp, MACM, SMIEEE, AFAIAA
Faculty of Information Technology
© 2024 Monash University

Why Study Graphical and Neural Processing Units?

1. Theory: GPGPUs and NPUs are now becoming ubiquitous – most

computers and smart devices in operation today use them
2. Theory: GPGPUs and NPUs employ highly parallel internal

architectures that are typically very different to multicore conventional
CPU designs

3. Practice: GPGPUs and now NPUs are mass produced for consumer
applications, ranging from gaming to photoediting and multimedia
display, and to accelerate AI apps – therefore cheap compute cycles;

4. Practice: Developing, debugging and testing GPGPU / NPU

applications requires a good understanding of fundamental ideas

2

Exponential Growth

Why GPGPUs and NPUs for High Performance Computing

▪ Moore’s Law is a factor in GPGPUs and NPUs, as it is in CPUs and
DRAMs used for main memory in machines (more in Topic 12);
▪ Moore’s Law describes “exponential growth” in density of integrated
circuit chips over time, with a “doubling period” of 18 – 24 months:
https://users.monash.edu/~ckopp/SYSTEMS/Moores-Law-0700.html;
https://archive.computerhistory.org/resources/access/text/2017/03/1027
70822-05-01-acc.pdf;

▪ A critical point is that the law applies to chip density – the Dennard

scaling “corollary” for clock speeds collapsed around 2006 when power
density limited further clock speed growth.

▪ While Moore’s Law is expected to slow in coming years it will not vanish

4

Impacts of Moore’s Law

▪ Transistor counts improve exponentially over time, but CPU clock speeds and

main memory speeds do not (any more); increasing numbers of cores in CPUs
or GPUs increase the demand for bandwidth to memory for accessing
instructions (code), but especially data in many applications;
In general purpose CPUs, the problem is addressed mainly by increasing the
size of the caches which the CPU uses to hold instructions and data and thus
minimise memory accesses;

▪

▪ Graphics applications are data intensive and not always well suited to caching
as the results of computations are not flushed periodically to memory, as is the
case with cached memory resident operands;

▪ The result is that GPUs have also employed a different model,  which is

essentially “data parallel”.

5

6

7

https://www.karlrupp.net/2018/02/42-years-of-microprocessor-trend-data/

8

Introduction to GPUs

GPU Concepts

▪ GPGPUs evolved from GPUs, GPUs evolved from “Graphics Accelerators”,

which in turn evolved from “Frame Buffers”;

▪ Frame buffers used a dual port memory which was accessed “upstream” by a

CPU which rendered graphical images in the frame buffers, which were pixel-by-
pixel read by a RAMDAC chip to generate VGA, SVGA, XGA or proprietary
analogue video for display on a Cathode Ray Tube monitor screen;

▪ The analogue interface has been replaced by digital DVI/HDMI or Thunderbolt

and the CRT by LCD, plasma, LCD/LED, LED or OLED displays;

▪ Early accelerators added dedicated hardware to perform common graphics
operations within the frame buffer memory, with the CPU programming the
accelerator with specific commands to execute;

▪ Modern GPGPUs follow the same model, driven by a CPU.

10

Cathode Ray Tube Display / Frame Buffer

Monochrome
CRT Display

Colour
CRT Display

11

1990s Frame Buffer Cards (S-Bus for Sun Microsystems Platforms)

12

1990s Frame Buffer Cards (S-Bus for Sun Microsystems Platforms)

Frame
Buffer

13

GPGPU Concepts – Frame Buffer

14

GPGPU Concepts – Accelerated Frame Buffer

15

GPGPU Concepts – Early GPU

16

GPGPU Concepts – GPU / CPU Integration (NVIDIA)

17

GPGPU Concept

▪ A GPGPU is a direct extension of the “traditional” GPU concept;
▪ Changes specific to GPGPUs are:
➢ programmable pipeline stages;
➢ higher precision integer arithmetic units;
➢ higher precision floating point arithmetic units;
▪ A GPGPU is thus able to perform types of computation not unique to

graphics processing, such as scientific computations;

▪ GPGPUs are still part of a “master-slave” SIMD (Single Instruction
Multiple Data) model, where a general purpose CPU sets up the
GPGPU cores to perform their computations.

18

GPGPU Memory Bandwidth

▪ A major performance limitation in current processors is the disparity in bandwidth
to main memory versus bandwidth to much smaller internal register storage;
In the time taken to read/write main memory, dozens of instructions can be
executed – providing the operands are in registers or cache;

▪

▪ GPUs employ dedicated very high bandwidth memory arrays for graphics

computations;

▪ GPUs use their internal dual ported memory to store operands, the intent is to
load data e.g. graphics bitmaps into the internal memory and conduct all
computational tasks within the GPU internal memory

▪ Recent GPGPUs will also employ caches inside the GPGPU between the

execution units and the fast internal memory

▪ Performance growth in GPGPU memory bandwidth follows Moore’s Law

19

GPGPU vs. SDRAM Memory Bandwidth

▪ A decade ago a conventional main memory system had a memory bandwidth

typically of Gigabytes/sec up to tens of Gigabytes/sec (AMD HT/HTX, Intel QPI)
– currently ~25 GB/s;

▪ Current DDR5 format Synchronous DRAM (SDRAM) has a bandwidth of around
32–64 GB/s, best case an improvement of around 2.6 X over the last decade;

▪ A decade ago a GPU/GPGPU internal memory subsystem had a memory

bandwidth of more than 100 Gigabytes/sec (Examples: NVIDIA Tesla @ 148
GB/s; AMD Radeon 5870 @ 153.6 GB/sec) – ~6 X better.

▪ The current generation of GPGPUs have internal memory subsystem bandwidths
~10 X faster, reflecting Moore’s Law exponential growth and widening the gap
between SDRAM and GPGPU bandwidth to ~16 X (NVIDIA GeForce RTX 4090
@ 1008 GB/s; AMD Radeon  RX 7900 XTX @ 960 GB/s)

20

High Bandwidth Memory

▪ High Bandwidth Memory (HBM) was developed initially for

GPUs but has since migrated to other challenging
applications, including AI platforms

▪ The basic concept in HBM is the vertical “stacking” of

memory chip dies to increase density – currently up to 8
dies may be stacked

▪ HBM uses a unique 1024 bit wide fast bus
▪ The JEDEC HBM3 standard delivers around 1,000 GB/s i.e.

1.0 TB/s bandwidth

21

High Bandwidth Memory

Micron

22

Fritz  Fritzchens

GPGPU/GPU versus Conventional CPU (NVIDIA)

Processor

GPGPU/GPU

CPU

Number of Cores

Hundreds-Thousands 2 - 12

Core complexity

Memory bandwidth

Vector operations

Low

~6:1

Yes

High

1:1

Limited or none

ILP Exploitation

Software scheduling

Control unit

Optimisation

Integration

Architecture

Software

Graphics/Scientific

General purpose

Master/Slave

Symmetrical MP

Data Parallel / Stream Conventional

CUDA / API

Any

23

GPGPU/GPU versus Conventional CPU (NVIDIA)

24

Example: NVIDIA Fermi GPGPU Die (2011)

▪ Fermi
▪ 3 billion transistors
▪ 512 x CUDA cores
▪ ~2 x the memory bandwidth cf

earlier chip

▪ L1 and L2 caches
▪ 8x the peak fp64 performance cf

earlier chip

▪ Error Correcting Code Memory
▪ C++ API

25

NVIDIA  AD102 Ada Lovelace GPGPU Die: (2022)

▪ Ada Lovelace architecture
▪ 76,300 billion transistors
▪ 18,432 x CUDA Cores
▪ 144 RT Cores
▪ 576 Tensor Cores (NPU)
▪ 576 Texture Units
▪ Memory bandwidth 960 GB/s
▪ L1 and L2 caches
▪ 91.1 FP32 TeraFLOPS

26

Alternatives: Intel Larrabee (Seiler 2008)
▪ The “conventional” GPGPU model is not the only feasible approach for a highly

▪

▪

parallel graphics processing architecture;
In 2007 – 2008 Intel prototyped the Larrabee chip, with eight cores, each based
on the x86 general purpose architecture, but with each core extended to perform
vector operations;
In practical terms this is a multicore general purpose processor optimised to
perform graphics or multimedia operations using vector operands;

▪ The advantage of the Larrabee model is that each core can perform general
computation independently and is not part of an asymmetric “master-slave”
model used with GPUs and GPGPUs;

▪ The disadvantage of the Larrabee model is performance loss for many problems

where the narrowly optimised GPGPU does better.

27

Example: Intel Larrabee Core

28

Example: Intel Larrabee Core

29

Example: Intel Larrabee-like Core Comparison

30

GPGPU Programming Models (Examples)

GPGPU Stream Processing Model

▪ Simplifies parallel software and hardware by restricting the parallel computation that can

be performed;

▪ A data set constitutes a (data) stream – common in multimedia and graphics

applications;

▪ A “graphics kernel” function is a series of operations;
▪ Commonly defined as a series of nested loops (with no data specification).
▪
▪ Uniform streaming, where the same kernel function is applied to all elements in the

The kernel function is applied to each element in the stream;

stream, is commonly used;

▪ Kernel functions are usually pipelined, and local on-chip or on card fast memory is

reused to minimize external memory bandwidth;

▪ Since the kernel and stream abstractions expose data dependencies, compiler tools

must fully automate and optimize on-chip management tasks.

32

Data Dependencies and Parallelism

▪ Two important stream data characteristics (as required by a

kernel):

✓ Independent
✓ Local
▪ Kernel operations define the basic data unit, both as input

and output.

▪ This allows the hardware to better allocate resources and

schedule global I/O.

33

Data Dependencies and Parallelism

▪ Definition of the data unit is usually explicit in the kernel, which is
expected to have well-defined (e.g. structures) inputs and outputs

▪ Well defined and independent compute blocks allow scheduling

of bulk I/O operations.

▪ Exploit the underlying hardware performance of cache and

specially GPGPU memory bus.

▪ Kernel locality: Values associated within a single kernel

invocation are made local and use the fast local GPGPU memory.

34

Stream Programming Languages

▪ Generally the following type of applications will benefit from stream computing:
✓ Compute Intensive: where the number of arithmetic operations is high for each

I/O or global memory access operation;

✓ Data Parallel: where the same kernel function can be applied repeatedly to
records in an input stream and a number of records can be processed
simultaneously without waiting for results from previous records (i.e. low data
dependencies);

✓ High Data Locality: where data is produced once, read once or twice later in the

application, and never read again. Intermediate streams passed between
kernels as well as intermediate data within kernel functions can capture this
locality directly using the stream processing programming model.

35

Common Stream Programming Languages

Open Standards:
✓ OpenCL (Open Computing Language) from Khronos Group, Inc;
✓ OpenACC from Cray Computer, NVIDIA,Portland Group, CAPS –

language extension via directives;

Vendor Proprietary Schemes:
✓ BROOK+ from AMD/ATI – based on open source Brook C/C++

compiler;

✓ Compute Unified Device Architecture (CUDA) from Nvidia. Only

available for NVIDIA GPUs;

36

Open CL

▪ Open specifications
✓ Designed for number crunching
✓ Parallel computing tasks
✓ Vendors specific implementations, must be complaint to the standard
▪ Proposed by Apple (includes Nvidia and Intel);
▪ Specifications developed by a number of companies (wider

following);

▪ Specifications maintained by Khronos Group, also provide license
fee free contents (KG also maintain the OpenGL specification)

37

Why Open CL?

▪ It is portable across implementations;
▪ Hardware heterogeneity, CPU, GPU support
▪ OpenCL Programming interface is designed to provide a good

framework to utilise underlying (parallel) hardware;

▪ Applications:
✓ Image, Video, Audio processing
✓ Gaming, Scientific calculations, Simulations
✓ Financial Modelling
✓ Computationally intensive data-parallel applications

38

OpenACC – Compiler Directive Extensions

▪ Open parallel programming standard designed to enable the scientific and

technical Fortran and C applications to use heterogeneous CPU/GPU computing
systems;

▪ OpenACC allows parallel programmers to provide simple hints, known as

“directives” to the compiler, identifying which areas of code to accelerate, without
requiring programmers to modify or adapt the underlying code itself (NVIDIA cite);
▪ Directives provide a common multi-platform and multi-vendor compatible scheme

which allows existing applications to be ported to GPUs with minimal effort;
▪ The intend behind OpenACC is therefore to provide a very low cost migration

path to GPU hardware for existing code written in languages such as Fortran or
C++;

▪ OpenACC relies on programmers understanding their code.

39

OpenACC Example: Calculating Pi (NVIDIA)

Fortran Version
program picalc

     implicit none
     integer, parameter ::

n=1000000

     integer :: i
     real(kind=8) :: t, pi
     pi = 0.0
 !$acc parallel loop
     do i=0, n-1
         t = (i+0.5)/n
         pi = pi + 4.0/(1.0 + t*t)
     end do
 !$acc end parallel loop
     print *, 'pi=', pi/n
 end program picalc

C Version
#include <stdio.h>

 #define N 1000000

 int main(void) {
 double pi = 0.0f; long i;
 #pragma acc parallel loop
 for (i=0; i<N; i++) {
    double t= (double)((i+0.5)/N);
    pi +=4.0/(1.0+t*t);
 }
 printf("pi=%16.15f\n",pi/N);
 return 0;
 }

40

NVIDIA CUDA (NVIDIA Website)

▪ Released by NVIDIA in 2006 - principal author Ian Buck who previously developed the

BROOK language;

▪ NVIDIA Performance Primitives (NPP) library includes over 2200 GPU-accelerated

functions for image & signal processing Arithmetic, Logic, Conversions, Filters, Statistics,
etc.

▪ Platforms: MS Windows 32/64; Linux 32/64; Mac OSX 32/64;
▪
cuFFT – Fast Fourier Transforms Library
▪
cuBLAS – Complete BLAS Linear Algebra Library
▪
cuSPARSE – Sparse Matrix Library
▪
cuRAND – Random Number Generation (RNG) Library
▪ NPP – Performance Primitives for Image & Video Processing
▪
Thrust – Templated Parallel Algorithms & Data Structures
▪ math.h - C99 floating-point Library

41

NVIDIA CUDA (CUDA C Programming Guide)

▪ CUDA C extends C by allowing the programmer to define C functions,
called kernels, that, when called, are executed N times in parallel by N
different CUDA threads, as opposed to only once like regular C
functions.

▪ A kernel is defined using the __global__ declaration specifier and the

number of CUDA threads that execute that kernel for a given kernel call
is specified using a new <<<…>>> execution configuration syntax.
▪ Each thread that executes the kernel is given a unique thread ID that is
accessible within the kernel through the built-in threadIdx variable;

▪ As an illustration, the following sample code adds two vectors A and B of

size N and stores the result into vector C:

42

NVIDIA CUDA (CUDA C Programming Guide)
// Kernel definition
__global__ void VecAdd(float* A, float* B, float* C)
{
int i = threadIdx.x;
C[i] = A[i] + B[i];
}
int main()
{
...
// Kernel invocation with N threads
VecAdd<<<1, N>>>(A, B, C);
...
}
// each of the N threads that execute VecAdd() performs one pair-wise addition.

43

NVIDIA CUDA (CUDA C Programming Guide)

▪ The CUDA programming model assumes that the CUDA threads execute on a
physically separate device that operates as a coprocessor to the host running
the C program.

▪ This is the case, for example, when the kernels execute on a GPU and the rest

of the C program executes on a CPU.

▪ The CUDA programming model also assumes that both the host and the device

maintain their own separate memory spaces in DRAM, referred to as host
memory and device memory, respectively.

▪ Therefore, a program manages the global, constant, and texture memory

spaces visible to kernels through calls to the CUDA runtime.

▪ This includes device memory allocation and deallocation as well as data transfer

between host and device memory.

44

Limitations in GPGPU Integration

▪ Most adaptations for GPGPU programming expose the GPGPU hardware to the

programmer;

▪ This presents two problems:
✓ Programmers often do not understand hardware performance well and make

mistakes, causing bugs or performance loss;

✓ Application portability can be compromised, as changing GPGPU hardware

vendors and/or APIs requires deep alterations to the code;

▪ Traditional “vectorising” Fortran compilers would hide the hardware from the

programmer to minimise the required programmer skills and maximise
application portability;

▪ Performance gains from using GPGPUs can be excellent, but rely upon the

programmer understanding the GPGPU design and GPGPU API library calls.

45

Problems in GPGPU Integration

▪ Scalability for GPGPU hosted applications is excellent, where the number of

cores N <= NGPGPU i.e. the number of cores on the GPGPU chip;

▪ This is because of the very high memory bandwidth to the private memory

▪

attached to the GPGPU chip;
If the required number of cores is higher, memory bandwidth between the
GPGPU modules is then much lower than memory bandwidth to the private
memory attached to the GPGPU chip;

▪ GPGPU solutions are therefore very sensitive to the data parallelism in the

▪

problem;
In systems with multiple GPGPUs, the main bus connecting the global system
main memory and GPGPUs becomes a critical system performance
bottleneck.

46

NPU Architectures

What is an NPU?

▪ Neural Processing Units (NPU) are specialized processors designed to
accelerate “Machine Learning Algorithms” or Artificial Neural Network
(ANN) models – colloquially “AI applications”

▪ While GPUs are frequently used for this purpose, NPUs are further

specialized for machine learning processing tasks and differ
architecturally

▪ GPNPUs are processors that are hybrids of a GPU and NPU
▪ NPUs may be designed as standalone processor chips for servers, or
as co-processors embedded in a SoC (System on a Chip) that may
include multiple conventional CPU cores, a GPU and an NPU

▪ Like GPUs, NPUs require a specialized programming model

48

Apple M2 Pro SoC - 16-core Neural Engine

▪ Apple M2 Pro SoC
▪ 12-core CPU with 8

performance cores and
4 efficiency cores

▪ 19-core GPU
▪ 16-core Neural Engine
▪ 200GB/s memory

bandwidth

▪ Proprietary API for

Apple Neural Engine

49

Intel Meteor Lake (Core Ultra) SoC -

▪ Intel Meteor Lake SoC
▪ Up to 6 Redwood Cove P-

cores

▪ 8 Crestmont E-cores
▪ 2 low power Crestmont LP

E-cores

▪ NPU with 4,000 8/16-bit

MAC (Multiply-
Accumulate) EUs
▪ GPU Intel Arc series

50

NPU Observations

▪ AI / ML applications are now widely used – e.g. natural language

processing, language translations, video and still image processing,
chatbots are common examples

▪ NPUs are designed to accelerate AI / ML applications
▪ AI / ML applications have to be developed by training a model with a

dataset, that requires very high higher precision floating point
performance to execute in reasonable time – backpropagation
calculations during training consume significant floating point operations

▪ Once an AI/ML model has been trained it can be deployed and used.
When an AI/ML model is running on an NPU this is termed inference

▪ NPUs for inference often use low precision FP or INT operations

51

Deep Neural Network (IBM Chart)

▪ Multiple hidden layers
▪ Each layer has an
array of emulated
neurons with multiple
inputs and outputs
▪ Input weights are

multiplied and results
added

▪ Multiply-Accumulate

operations

52

Google TPU (Tensor Processing Unit) Series

▪ Proprietary TPUv1 announced in 2016, evolving through to the

TPUv5e/p in 2023

▪ Designed initially for Google datacenters to accelerate server AI/ML

applications

▪ TPUv5p has 95 GB of HBM and a memory bandwidth of 2,765

GB/s (2.765 TB/s)

▪ Cited performance is 918 8-bit Integer TOPS (Tera Operations Per

Second)

▪ The TPU architecture is also used in the Pixel Neural Core that

was developed for consumer applications like the Pixel 4 and Pixel
4 XL smartphones

53

Example: Google TPU – Systolic Array Processor

▪ The Google TPU is an example of a Systolic Array Processor
▪ The Systolic Array Processor scheme was devised in 1984 by Richard
Brent @ANU (a Monash graduate) and H.T. Kung @ CMU to solve a
range of greatest common divisor polynomial problems

▪ The Google development team led by Norman Jouppi adapted this

model for the TPU design to produce an architecture optimized for fast
inference using Deep Neural Networks (DNN)

▪ The TPU is designed for (runtime) inference using low precision integer
arithmetic, while the DNN model training would be performed using
GPUs and high precision floating point arithmetic
▪ Google named this device the Tensor Processing Unit

54

Flynn’s Taxonomy

▪ Multiple Instruction Single Data

55

Example: Google Tensor Processing Unit (1)

▪

▪ Norman P. Jouppi, et al. 2017. In-Datacenter Performance Analysis of a
Tensor Processing Unit. In Proceedings of the 44th Annual International
Symposium on Computer Architecture (ISCA '17). Association for Computing
Machinery, New York, NY, USA, 1–12.
https://doi.org/10.1145/3079856.3080246
“Neural networks (NN) target brain-like functionality and are based on a simple
artificial neuron: a nonlinear function (such as max(0, value)) of a weighted
sum of the inputs. These pseudo neurons are collected into layers, with the
outputs of one layer becoming the inputs of the next in the sequence. The
“deep” part of DNN comes from going beyond a few layers, as the large data
sets in the cloud allowed more accurate models to be built by using extra and
larger layers to capture higher levels of patterns or concepts, and GPUs
provided enough computing to develop them.”

56

Example: Google Tensor Processing Unit (2)

▪

▪

“The two phases of NN are called training (or learning) and inference (or
prediction), and they refer to development versus production. The developer
chooses the number of layers and the type of NN, and training determines the
weights. Virtually all training today is in floating point, which is one reason GPUs
have been so popular. A step called quantization transforms floating point
numbers into narrow integers—often just 8 bits—which are usually good enough
for inference.”
“Rather than be tightly integrated with a CPU, to reduce the chances of delaying
deployment, the TPU was designed to be a coprocessor on the PCIe I/O bus,
allowing it to plug into existing servers just as a GPU does. Moreover, to simplify
hardware design and debugging, the host server sends TPU instructions for it to
execute rather than the TPU fetching them itself. Hence, the TPU is closer in
spirit to an FPU (floating-point unit) coprocessor than it is to a GPU.”

57

Example: Google Tensor Processing Unit (3)

58

Example: Google Tensor Processing Unit (4)

Systolic Flow in TPU

TPU installed on PCI-E Card

➞

s
u
B
E
-
I

C
P
e
n
a
L

6
1

59

Example: Google Tensor Processing Unit (5)

▪

▪

“The goal was to run whole inference models in the TPU to reduce
interactions with the host CPU and to be flexible enough to match the NN
needs of 2015 and beyond, instead of just what was required for 2013 NNs.”
“The TPU instructions are sent from the host over the PCIe Gen3 x16 bus into
an instruction buffer. The internal blocks are typically  connected  together by
256-byte-wide paths.  Starting in the upper-right corner, the Matrix Multiply
Unit is the heart of the TPU. It contains 256x256 MACs [Multiply-Accumulate
Units] that can perform 8-bit multiply-and-adds on signed or unsigned
integers. The 16-bit products are collected in the 4 MiB of 32-bit Accumulators
below the matrix unit. The 4 MiB holds 4096, 256-element, 32-bit
accumulators. The matrix unit produces one 256-element partial sum per
clock cycle.”

60

Example: Google Tensor Processing Unit (6)

▪

▪

“When using a mix of 8-bit weights and 16-bit activations (or vice versa), the
Matrix Unit computes at half-speed, and it computes at a quarter-speed when
both are 16 bits. It reads and writes 256 values per clock cycle and can perform
either a matrix multiply or a convolution. The matrix unit holds one 64 KiB tile of
weights plus one for double buffering (to hide the 256 cycles it takes to shift a tile
in). This unit is designed for dense matrices.”
“The weights for the matrix unit are staged through an on chip Weight FIFO that
reads from an off-chip 8 GiB DRAM called Weight Memory (for inference,
weights are read-only; 8 GiB supports many simultaneously active models). The
weight FIFO is four tiles deep. The intermediate results are held in the 24 MiB
on-chip Unified Buffer, which can serve as inputs to the Matrix Unit. A
programmable DMA controller transfers data to or from CPU Host memory and
the Unified Buffer.”

61

NPU Programming Models

Software Development Models for Artificial Neural Networks (1)

▪ Conventional software development involves the design and coding of

applications in one or another high level programming language, and some
translation mechanism into machine code in a specific instruction set for a
specific architecture

▪ The most common approach is the use of a language compiler or interpreter to

▪

generate machine code
“AI applications” i.e. Machine Learning Algorithms or Artificial Neural Network
(ANN) models employ a similar approach, but with differences

▪ This is because such applications require the training of the model to generate

the required weights for the ANN to operate

▪ The developed and trained ANN model amounts to a graph (mesh) of links

between emulated neurons, and weights for these links

63

Software Development Models for Artificial Neural Networks (2)

▪ Given an AI model that is a graph of links between emulated neurons, and

▪

weights for these links, this model must be translated into an executable form
In practice AI models can be executed on traditional RISC/CISC architecture
CPUs, a wide range of GPUs, and a wide range of specialized NPUs with a
range of different proprietary architectures.

▪ The use of compilers is the preferred approach at this time, to translate a model

in a specific format into CPU, GPU or NPU machine instructions of a type
specific to the hardware intended to be used so it can execute inference ops.

▪ The development cycle for an AI app therefore follows a different flow to a

traditional high level language app

▪ Design of model ➞ training dataset collection and labelling ➞ training of model
➞ compilation of model into hardware specific machine instructions and data

64

OpenXLA (Accelerated Linear Algebra) Compiler (1)

▪ Cited from https://openxla.org/xla/architecture - “XLA (Accelerated Linear Algebra) is a

machine learning (ML) compiler that optimizes linear algebra, providing improvements in
execution speed and memory usage.” Objectives:

1.

2.

3.

4.

“Improve execution speed. Compile subgraphs to reduce the execution time of short-
lived ops and eliminate overhead from the runtime, fuse pipelined operations to reduce
memory overhead, and specialize known tensor shapes to allow for more aggressive
constant propagation.”
“Improve memory usage. Analyze and schedule memory usage, eliminating many
intermediate storage buffers.”
“Reduce reliance on custom ops. Remove the need for many custom ops by
improving the performance of automatically fused low-level ops to match the
performance of custom ops that were originally fused by hand.”
“Improve portability. Make it relatively easy to write a new backend for novel hardware,
so that a large fraction of ML models can run unmodified on that hardware.”

65

OpenXLA (Accelerated Linear Algebra) Compiler (2)

▪ Cited from https://openxla.org/xla/architecture - “The XLA compiler takes model graphs

from ML frameworks defined in StableHLO and compiles them into machine instructions for
various architectures. StableHLO defines a versioned operation set (HLO = high level
operations) that provides a portability layer between ML frameworks and the compiler.”
“XLA performs several built-in optimization and analysis passes on the StableHLO graph
that are target-independent, such as CSE, target-independent operation fusion, and buffer
analysis for allocating runtime memory for the computation. During this optimization stage,
XLA also converts the StableHLO dialect into an internal HLO dialect.”
“XLA sends the HLO computation to a backend for further HLO-level optimizations, this
time with target-specific information and needs in mind. For example, the GPU backend
may perform operation fusions that are beneficial specifically for the GPU programming
model and determine how to partition the computation into streams.”
“The backend then performs target-specific code generation. The CPU and GPU backends
included with XLA use LLVM for low-level IR, optimization, and code generation.”

1.

2.

3.

66

OpenXLA (Accelerated Linear Algebra) Compiler (3)

▪ Code generated by an XLA compiler for a specific type of hardware will then

have to be loaded into the hardware for execution to take place

▪ Loading of the generated code takes place in a manner that is specific to the

hardware – which might be a CPU, GPU or NPU

▪ Once the machine format compilation of the model is done, the model can be

executed and perform inferences on input data

▪ Portability was a key agenda for the developers of XLA
▪ ANN models built using a range of languages and frameworks can be translated
into StableHLO language and then compiled using XLA into a machine level
format specific to the hardware to be used for inference

▪ Support exists for tiling a model across multiple NPUs to increase performance
▪

In effect, NPUs can be operated in a manner similar to parallel CPUs

67

Summary

Summary
▪ Moore’s Law
▪ GPU Concepts / Evolution, GPGPU Concepts
▪ Memory bandwidth considerations;
▪ GPGPUs versus Multicore CPUs;
▪ Stream Processing in GPGPUs;
▪ Data dependencies and parallelism;
▪ Stream programming languages; OpenCL, OpenACC, BROOK+, CUDA;
▪ Limitations/Problems in GPGPU Integration;
▪ Neural Processing Units (NPUs)
▪ Programming models for NPUs

69

Reading Materials

Reading and References

▪ Owens, J., Ch.29 Streaming Architectures and Technology Trends, GPU Gems 2: Part IV -

General-Purpose Computation on GPUS: A Primer, NVIDIA Corp, URI:
http://developer.nvidia.com/book/export/html/48

▪ Seiler, L. et al, Larrabee: A Many-Core x86 Architecture for Visual Computing, ACM

Transactions on Graphics, Vol. 27, No. 3, Article 18, August 2008.
▪ NVIDIA CUDA , NVIDIA CUDA C Programming Guide, 4/16/2012;
▪ NVIDIA: Tesla Personal Supercomputer; URI:

▪

http://www.youtube.com/watch?v=l8FUmS1h-5U
TYAN B7015 -- 8-GPU Nvidia Tesla Supercomputer Server, URI:
http://youtu.be/VML2cXDe32M

▪ Norman P. Jouppi, et al. 2017. In-Datacenter Performance Analysis of a Tensor

Processing Unit. In Proceedings of the 44th Annual International Symposium on Computer
Architecture (ISCA '17). Association for Computing Machinery, New York, NY, USA, 1 –12.
https://doi.org/10.1145/3079856.3080246

71

